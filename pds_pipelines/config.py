import os
# Database credentials
credentials = {'test': {'user': 'upcmgr',
                        'pass': '',
                        'host': 'localhost',
                        'port': '5432',
                        'db': 'upc'}
               }

# Redis path(?) info
redis_info = {'host': 'localhost', 'port': '6379', 'db': '0'}
